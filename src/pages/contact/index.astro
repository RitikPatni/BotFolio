---
import BaseLayout from '../../layouts/BaseLayout/BaseLayout.astro';
import { SITE_CONFIG } from '../../config/site';
import SocialIcon from '../../components/SocialIcon.astro';
import './_contact.scss';
---

<BaseLayout title="Contact — Ritik Patni" path="/contact">
  <div class="contact">
  <section class="contact__hero">
    <h1 class="contact__title">Contact</h1>
    <p class="contact__lead">
      Open to conversations around frontend/product engineering, design-minded web experiences,
      and photography collaborations.
    </p>
  </section>

  <section class="contact__card">
    <h2 class="contact__card-title">Find me online</h2>
    <div class="contact__socials" aria-label="Social links">
      {SITE_CONFIG.socials.map((social) => (
        <a class="contact__social-link" href={social.href} target="_blank" rel="noreferrer noopener">
          <SocialIcon name={social.icon} class="contact__social-icon" />
          {social.label}
        </a>
      ))}
    </div>
  </section>
  </div>
</BaseLayout>
